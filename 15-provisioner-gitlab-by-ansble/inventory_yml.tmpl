all:
  children:
    gitlab:
      hosts:
        "${hostname}":
          ansible_host: "${public_ip}"
  vars:
    gitlab_domain: "${gitlab_domain}"
    ansible_user:  ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
